% use URI::Find::Schemeless;
% use Mojo::Util qw/html_escape/;
% my $finder = URI::Find::Schemeless->new(
%             sub { my($uri, $orig_uri) = @_; return join('','<a target="_blank" href="', 
%                   html_escape($uri), '">', html_escape($orig_uri) , '</a>'); 
%         });
% $finder->find( \$string, \&Mojo::Util::html_escape);
<%== $string %>
